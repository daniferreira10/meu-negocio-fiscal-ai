
// Este arquivo é mantido por compatibilidade, mas agora importa os tipos do arquivo principal
import { 
  ProfileType, 
  MaritalStatus, 
  cpfRegistrationSchema, 
  CpfFormValues, 
  RegistrationFormProps 
} from '@/types/userProfileTypes';

export { ProfileType, MaritalStatus, cpfRegistrationSchema };
export type { CpfFormValues, RegistrationFormProps };
